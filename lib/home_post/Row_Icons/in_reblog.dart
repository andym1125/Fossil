import 'package:flutter/material.dart';

class InReblog extends StatelessWidget {
  
  const InReblog({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(
      Icons.rocket_launch,
      color: Colors.blue,
    );
  }
}