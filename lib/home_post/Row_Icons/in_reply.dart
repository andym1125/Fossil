import 'package:flutter/material.dart';

class InReplyTo extends StatelessWidget {
  
  const InReplyTo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(
      Icons.reply,
      color: Colors.blue,
    );
  }
}